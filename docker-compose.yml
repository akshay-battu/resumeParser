services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      CORS_ORIGINS: http://localhost:5173,http://localhost:5000
    volumes:
      - ./backend:/app/backend
    restart: unless-stopped
